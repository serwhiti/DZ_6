# Напишите программу для сжатия данных по следующему алгоритму: 
# s = 'aaaabbсaa' преобразуется в 'a4b2с1a2', то есть группы одинаковых символов исходной строки заменяются на этот символ и количество его повторений в этой позиции строки.
# Программа считывает из текстового файла строку, кодирует её предложенным алгоритмом и выводит закодированную последовательность в другой текстовый файл. 

f = open('1.txt')
s = f.readline()
count = 0
sh = ''
for i in range(len(s)):
    sim = s[i]
    count += 1
    if i == len(s) - 1:
        sh = sh + s[i] + str(count)
        break
    if s[i]!=s[i+1]:
        sh = sh + s[i] + str(count)
        count=0
print(sh)
